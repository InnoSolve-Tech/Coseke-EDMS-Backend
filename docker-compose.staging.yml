services:
  edms-gateway:
    image: benzidarwin/staging-gateway:latest
    ports:
      - "8788:8787" # Exposed port is different for staging
    depends_on:
      - file-management
      - workflows

  file-management:
    image: benzidarwin/staging-file-management:latest
    ports:
      - "8083:8081" # Exposed port is different for staging

  workflows:
    image: benzidarwin/staging-workflows:latest
    ports:
      - "8084:8082" # Exposed port is different for staging
